---
import { site } from '../data/site';
---

<nav class="flex flex-wrap gap-2">
  {site.socials.map((s) => (
    <a href={s.href} target="_blank" rel="me noopener" class="sochip">
      {s.label}
    </a>
  ))}
</nav>
